import styled from 'styled-components';

export const HomeTrendsTite = styled.h2(`
    font-family: Gill Sans, Verdana;
	font-size: 301px;
	line-height: 14px;
	text-transform: uppercase;
	letter-spacing: 2px;
	font-weight: bold; `);
